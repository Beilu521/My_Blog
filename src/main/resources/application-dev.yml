# 服务器端口
server:
  port: 8080

# 数据库配置（本地MySQL）
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: "123456"  # 你本地MySQL的root密码

  # Redis配置（本地Redis）
  data:
    redis:
      host: localhost
      port: 6379
      database: 0

  # 邮箱配置（用QQ邮箱/163邮箱都可以）
  mail:
    host: smtp.qq.com  # 163邮箱是smtp.163.com
    username: 383008278@qq.com
    password: epvexkcgwnaabjhd
    default-encoding: UTF-8

# MyBatis-Plus配置
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true  # 下划线转驼峰
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl  # 开发时打印SQL日志

# 自定义配置
jwt:
  secret: My_Blog_Beilu520_Beilu521
  access-expire: 7200000    # Access Token 过期时间：2小时
  refresh-expire: 604800000 # Refresh Token 过期时间：7天