server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # mysql是Docker里的服务名，不用改IP
    url: jdbc:mysql://mysql:3306/blog?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: "123456"  # 和Docker里MySQL的密码保持一致

  data:
    redis:
      host: redis  # redis是Docker里的服务名，不用改IP
      port: 6379
      database: 0

  mail:
    host: smtp.qq.com
    username: 383008278@qq.com
    password: epvexkcgwnaabjhd
    default-encoding: UTF-8

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
    # 生产环境关闭SQL日志
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl

# 自定义配置
jwt:
  secret: My_Blog_Beilu520_Beilu521
  access-expire: 7200000    # Access Token 过期时间：2小时
  refresh-expire: 604800000 # Refresh Token 过期时间：7天